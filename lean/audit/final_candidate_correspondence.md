# Independent current-source correspondence review

**Layer B: ACCEPTED.** This is a source-to-Lean correspondence verdict, separate from final kernel closure, cold replay, mutation results, and release authority.

Reviewed denominator: **12 named results, 50 theorem-strength interfaces, 34 definitions, plus 10 affected/new interfaces**. Every row has exact current source text/spans, current theorem/definition IDs, inherited provenance, and an explicit correspondence status in the JSON. All 159 files in the inherited reviewed source closure remain byte-identical. No baseline scope was dropped.

The six changed named statements preserve the exact prescribed width and frame supremum, full density interval 0<theta<1, arbitrary probability frame laws with conditional upper sampling assumptions, and all-k Markov bounds for every epsilon>0. The explicit constant remains C=200*2^20000+8196*(576*4^24). Full sampling and n=1 are covered. General spectral transfer retains delta as an independent parameter.

The initial literal-interface findings are now resolved: the fixed-(d,h) full-density entry contribution uses the exact absolute-product definition; the theta(1-theta) selector intermediate is explicit; the saturated upper Loewner chain is encoded for every quadratic form; and the stronger saturated gamma+exp(-epsilon²k/32) estimate is proved for arbitrary joint laws without any upper draw. The conditional joint theorem imposes upper measurability, independence, law, and failure assumptions only when qplus<1. Equality qplus=1 belongs to the saturated case.

A fresh source-first reverse pass found no remaining mathematical source repair or added load-bearing hypothesis. Two implementation representations are compatible with the written proof: the saturated internal density-one full sample is deterministic while qplus is only an algebraic scaling parameter, and the universal quadratic-form chain expresses Loewner order for the symmetric matrices at hand. The old selected-width theorem is not used to infer prescribed-width success. Private signed-trace helpers are explicitly mapped for the finite equality, cumulant, selector and geometric-sum paths.

RA0–RA4 meanings were not available in the local references; as agreed with the controller, this report uses factual classifications rather than inventing those labels. No author-only comprehension/disclosure attestation or release approval is included.

Current actual type/dependency/axiom evidence, exact private-ID resolution, semantic mutation results, a cold project rebuild, and fresh kernel replay remain controller mechanical checks. The source snapshot hashes in the JSON bind this Layer B verdict; mathematical changes after that snapshot require re-review.
